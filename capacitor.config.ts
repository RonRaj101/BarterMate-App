import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'barter-mate-draft',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
